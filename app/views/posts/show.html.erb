<!-- AM: Converting the below link to a link helper would make your code more readable (and consistent since you use them elsewhere!) --> 
<h2><%= @post.name %> <a href="/posts/<%= @post.id %>/edit">(edit)</a></h2>
<h4><%= @post.content %></h4>

<h3>Comments <%= link_to "(+)", new_post_comment_path(@post) %></h3>
<ul>
  <% @post.comments.each do |comment| %>
      <h2><%= comment.name %></h2>
      <p> <%= comment.content %></p>
  <% end %>
</ul>
